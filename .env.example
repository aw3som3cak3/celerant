# Copy to .env.local. Used to sign session cookies. Any long random string.
SESSION_SECRET=change-me-to-a-long-random-string
# Where the sqlite file lives. Defaults to ./data/celerant.db
DATABASE_PATH=./data/celerant.db
